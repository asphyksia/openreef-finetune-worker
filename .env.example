# Example only — never commit real secrets.
# Used when expanding docker-compose-*.yml for a local source stack.

PROVIDER_PRIVATE_KEY=
OPENREEF_SIGNER_TOKEN=
OPENREEF_API_BASE=https://openreef.network
OPENREEF_CHAIN_ID=
OGPU_USE_TESTNET=
HF_TOKEN=
PORT=5555
CALLBACK_URL=

# After smoke, pin digests (see OpenReef docs/known-good-image-digests-*.json):
# FINETUNE_IMAGE=ghcr.io/asphyksia/finetune-worker@sha256:...
# OPENREEF_SIGNER_IMAGE=ghcr.io/asphyksia/openreef-signer@sha256:...
